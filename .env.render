# Environment Configuration for Render Deployment

# Hugging Face Configuration (FREE)
USE_HUGGINGFACE=true
HUGGINGFACE_API_KEY=your_hf_token_here
HF_MODEL=mistralai/Mistral-7B-Instruct-v0.2

# Embeddings (runs locally on Render)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Disable local models (Ollama not available on Render free tier)
USE_LOCAL_MODELS=false

# Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
GRADIO_PORT=7860

# HOW TO GET HUGGING FACE API KEY (100% FREE):
# 1. Go to: https://huggingface.co/settings/tokens
# 2. Click "New token"
# 3. Select "Read" access
# 4. Copy the token (starts with hf_)
# 5. Paste it above as HUGGINGFACE_API_KEY value
# 6. In Render dashboard, add as environment variable (NOT committed to Git)
