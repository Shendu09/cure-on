version: '3.8'

services:
  medical-rag-chatbot:
    build: .
    container_name: medical-rag-chatbot
    ports:
      - "8000:8000"    # FastAPI
      - "7860:7860"    # Gradio
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - EMBEDDING_MODEL=${EMBEDDING_MODEL:-text-embedding-3-small}
      - LLM_MODEL=${LLM_MODEL:-gpt-4o-mini}
      - TEMPERATURE=${TEMPERATURE:-0.1}
      - TOP_K=${TOP_K:-5}
      - CHUNK_SIZE=${CHUNK_SIZE:-1000}
      - CHUNK_OVERLAP=${CHUNK_OVERLAP:-200}
    volumes:
      - ./data:/app/data
      - ./src:/app/src
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    command: python src/app_gradio.py

  # Optional: Run FastAPI separately
  # api:
  #   build: .
  #   container_name: medical-rag-api
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - OPENAI_API_KEY=${OPENAI_API_KEY}
  #   volumes:
  #     - ./data:/app/data
  #   command: python src/app_api.py
